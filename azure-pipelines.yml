trigger:
- master
jobs:
- job: 'test_basic'
  pool:
    name: Default
  steps:
  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        # Write your commands here
        
        echo 'Hello world' > file01
        cat file01
      workingDirectory: '/tmp'